/*var twitter;
beforeEach(function() {
    //mock twitter API lib
    var Codebird = window.Codebird  = function() {};
    Codebird.prototype = {
        setConsumerKey: angular.noop,
        __call: angular.noop
    };
    module('twitter');
    inject(function($injector) {
        twitter = $injector.get('twitter');
    });
});
describe('twitter api client tests', function() {
    it("should authorise on twitter and resolve api function into promise", function() {
        debugger;
        var successCallback = angular.noop
        twitter.prepare('test_key', 'test_secret').then()
    });
})*/
